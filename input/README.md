# Input

This folder contains the compact starting inputs retained for audit context.

- `raw/` contains the original compact CCS/control tables used as starting material.
- `original/` contains the original corrected CCS report PDF that preceded the final report.

These files are inputs, not the final analytical deliverables. Final reports, figures, and reader-facing result tables are under `output/`.
